.header
  = render 'header'
.contents
  .pictures
    .slick-prev
    .picture.slick
      = image_tag @picture[0].image.url
      = image_tag @picture[1].image.url
      = image_tag @picture[2].image.url
  .posts
    =render @tweet
    .paginate
      = paginate(@tweet)